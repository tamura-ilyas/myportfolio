import React from "react";
import ReactDOM from "react-dom/client";
import SoftwarePortfolio from "./portfolio";
import "./index.css";
ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <SoftwarePortfolio />
  </React.StrictMode>,
);
